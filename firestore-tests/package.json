{
  "name": "exm-todo-firestore-tests",
  "private": true,
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore \"cd firestore-tests && npx jest --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.0",
    "jest": "^29.7.0"
  }
}
