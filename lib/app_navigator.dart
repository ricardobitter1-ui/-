import 'package:flutter/material.dart';

/// Navigator raiz do [MaterialApp] — usado para SnackBars e modais fora do contexto local.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
